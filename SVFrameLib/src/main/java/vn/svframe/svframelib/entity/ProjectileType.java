package vn.svframe.svframelib.entity; import vn.svframe.svframelib.skill.trigger.TriggerType; public enum ProjectileType { ARROW,TRIDENT; private final TriggerType tick=new TriggerType(name()+"_TICK"),hit=new TriggerType(name()+"_HIT"),land=new TriggerType(name()+"_LAND"); public TriggerType getTickTrigger(){return tick;} public TriggerType getHitTrigger(){return hit;} public TriggerType getLandTrigger(){return land;} }
