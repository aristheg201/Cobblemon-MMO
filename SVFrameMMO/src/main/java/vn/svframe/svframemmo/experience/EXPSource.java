package vn.svframe.svframemmo.experience;

/** Identifies where progression experience originated without coupling the core to integration mods. */
public enum EXPSource {
    SOURCE,
    API,
    COMMAND,
    VANILLA,
    PROFESSION_TO_CLASS,
    OTHER
}
